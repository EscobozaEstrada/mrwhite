version: 1
# Amplify build specification for frontend monorepo
applications:
  - appRoot: frontend
    frontend:
      phases:
        preBuild:
          commands:
            - echo "📦 Installing frontend dependencies..."
            - npm ci
        build:
          commands:
            - echo "🔨 Building Next.js application..."
            - npm run build
            - echo "✅ Build complete"
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
